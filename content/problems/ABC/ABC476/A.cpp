#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;

int main() {
    string S;
    cin >> S;
    if (S.back() == 'e'){
        cout << S << 'r' << endl;
    } else {
        cout << S << "er" << endl;
    }
}
