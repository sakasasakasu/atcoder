#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;

int main() {
    int X;
    cin >> X;
    int ans[4] = {0, 2, 3, 1};
    cout << ans[X] << endl;
    return 0;
}
